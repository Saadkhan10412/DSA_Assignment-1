class Solution {
public:
    int singleNonDuplicate(vector<int>& nums) {
        return help(nums, 0, 0);
    }

    int help(vector<int>& nums, int i, int rec){
        if(i == nums.size()) return rec; 
        return help(nums, i + 1, rec ^ nums[i]);
    }
};
